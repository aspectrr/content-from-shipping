{
  "name": "cfs-cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "CLI bridge: pi pushes episodes, drafts, finals, and lessons into the content web app.",
  "bin": {
    "cfs": "./src/index.ts"
  },
  "scripts": {
    "cfs": "bun run src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  }
}
